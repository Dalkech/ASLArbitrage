import Score from "../components/score/score";

export default class App
{
    public Score: Score = new Score();
    /**
     *
     */
    constructor() {
        console.log("hello from SSLarbitrage Welcome, Setting up is over ");
    }
}